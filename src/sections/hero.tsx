import Navbar from "../components/navbar";
import "../styles/sections/hero.scss";

const Hero = () => {
  return (
    <div className='hero section'>
      <Navbar />
      hero
    </div>
  );
};

export default Hero;
